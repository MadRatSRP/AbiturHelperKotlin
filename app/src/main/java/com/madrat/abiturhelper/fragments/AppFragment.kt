package com.madrat.abiturhelper.fragments

import android.os.Bundle
import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup
import androidx.appcompat.app.AppCompatActivity
import androidx.fragment.app.Fragment
import androidx.navigation.Navigation
import com.madrat.abiturhelper.R
import kotlinx.android.synthetic.main.fragment_app.*

class AppFragment: Fragment() {

    /*override fun onActivityCreated(savedInstanceState: Bundle?) {
        super.onActivityCreated(savedInstanceState)
        //setupMVP()

        setupSpecialties.setOnClickListener {view->
            //Navigation.findNavController(view).navigate(R.id.action_appFragment_to_setupScore)}

        countChancesOfGraduation.setOnClickListener {  }
    }

    override fun onCreateView(inflater: LayoutInflater, container: ViewGroup?,
                              savedInstanceState: Bundle?): View? {
        (activity as AppCompatActivity).supportActionBar?.setTitle(R.string.textStartScreen)
        return inflater.inflate(R.layout.fragment_app, container, false)
    }*/
}